# decisions_log.md — архитектурные решения

**Сфера:** решения уровня архитектуры, стека и скоупа. Не журнал задач и не changelog.
**Обновлено:** 2026-09-11.

Формат: решение — почему отвергнута альтернатива. Запись пересматривается через 3 месяца
или при наступлении условия в колонке «Пересмотр». Устаревшие записи удаляются, а не архивируются.

## Активные решения

| Дата | Решение | Почему не альтернатива | Пересмотр |
|---|---|---|---|
| 2026-09-03 | Скоуп MVP — два слоя: скрининг (V4-Flash) и аудит (V4-Pro). Слоёв 0 и 3 нет | Четырёхслойная схема разводила усилия на оркестрацию, пока ядро (NL → релевантные репо) оставалось нераскрытым. Каскад из двух слоёв даёт всю экономию токенов; третий слой добавляет цену и задержку без замеренной пользы | после замера доли неверных вердиктов на golden-set |
| 2026-09-03 | Генерация 5–10 GitHub-запросов из интента — отдельный документированный компонент (`QUERIES.md`) | Один запрос к нессемантическому полнотекстовому поиску пропускает проекты, чей README сформулирован иначе. Это главный источник потерь, и он не лечится ниже по конвейеру: Слой 1 и Слой 2 умеют только отсеивать | после диагностики на 20–30 реальных запросах |
| 2026-09-03 | Строки запросов собирает детерминированный код, модель даёт только интент и синонимы | Модель, пишущая строку запроса целиком, ломает воспроизводимость и регулярно порождает невалидный синтаксис квалификаторов | — |
| 2026-09-03 | Слияние выдач через RRF (k=60) + метаданный приор, веса 0,70/0,30 | Скоры `best-match` и `stars` несопоставимы между собой; калибровать их не на чем. RRF работает с рангами и не требует калибровки | при изменении весов — обязательный замер recall@5 до/после |
| 2026-09-03 | Кэш на уровне репозитория: `repo_id + head_sha` → `AuditResult`. Инвалидация по смене SHA, TTL по времени нет | Кэш по хешу NL-запроса даёт околонулевой hit-rate: запросы не совпадают побайтово. Один репозиторий, наоборот, попадает в выдачу многих разных запросов. TTL по времени либо отдаёт устаревшее, либо жжёт токены на неизменившемся коде | — |
| 2026-09-03 | Работа только через GitHub API. Ни клонирования, ни запуска чужого кода | Запуск произвольного репозитория — исполнение недоверенного кода, причём уже на стадии установки зависимостей. Одного Docker для изоляции недостаточно (нужны no-network, read-only fs, drop caps, лимиты pids). При этом в MVP большинство репозиториев просто не соберётся — цена высокая, отдача близка к нулю | при диагнозе «болит аудит» из-за нехватки сведений о поведении |
| 2026-09-03 | Лицензия — атрибут отчёта, а не фильтр поиска | Фильтр `license:` на входе выбрасывает лучшие проекты в нишах, где сильные решения под GPL. Лицензия влияет на вердикт (`code_reuse_allowed`), но не на попадание в выдачу | — |
| 2026-09-03 | Разделение контекстов: аудит читает код и отдаёт только структурированные факты; генерация кода исходников не видит. Плюс автодетектор фрагментов >120 символов | Идеи и архитектура авторским правом не охраняются, но один общий контекст LLM, который сначала читает GPL-код, а потом пишет свой, оставляет риск дословной регургитации. Разделение — техническая реализация clean-room, а не декларация | — |
| 2026-09-03 | Метрика качества — recall@5 на golden-set из 20–30 задач, цель ≥ 60% | «Точность ≥ 80% на пилоте из 5 задач» неизмерима: пять задач дают шаг 20%. Сравнение экономии токенов с «50 репо через одну большую модель» — сравнение с тем, чего никто не делает; корректная база — тот же конвейер без каскада | после первого замера |
| 2026-09-03 | Модель Слоя 2 — `deepseek-v4-pro`; `qwen3.8-max` только как fallback при 5xx | V4-Pro дешевле при том же контексте 1M: 0,66/1,98 off-peak против 2,00/6,00 у Qwen. Держать обе как равноправные значило иметь непредсказуемую стоимость запуска | ежемесячная сверка прайса |
| 2026-09-03 | Синхронный режим по умолчанию; `--off-peak` — явный опт-ин с отдельным SLA (≤ 12 ч против ≤ 5 мин) | Откладывание по умолчанию противоречит требованию «полный цикл < 5 минут». Это два разных режима с разными обещаниями, а не один компромисс | — |
| 2026-09-03 | Автономно агенту разрешены только запись/инвалидация кэша, повтор после rate-limit и дозапись provenance | Автоправка промптов и весов на потоке в 1–5 пользователей меняет систему быстрее, чем накапливаются данные, чтобы отличить улучшение от шума. Ожидаемая ценность отрицательна | ≥ 500 прогонов и ≥ 5 регулярных пользователей |
| 2026-09-03 | `README.md` (вход для человека) и `CHECKLIST.md` (гейт PR) добавлены в набор документации. Числового правила «строго N файлов» нет; действует: одна сфера на файл, ≤ 200 строк (кроме `SCHEMAS.md`), новый файл только вместе с записью в этом логе. Актуальный состав — в таблице `CLAUDE.md` | Решение пользователя. `README` закрывает сценарий «пришёл человек, а не агент»: `CLAUDE.md` для этого не годится, он контракт с агентом. `CHECKLIST` фиксирует то, чего не видит автоматика: замер recall@5 при правке ранжирования, согласованность чисел между файлами, отсутствие клонирования в diff. Дублирования с pre-commit и Action нет — эти гейты вынесены в отдельный раздел «вручную не дублировать» | — |
| 2026-09-03 | Добавлен `PROGRESS.md` — точка возобновления работы. Всего 9 файлов документации | Сессия с агентом обрывается и начинается заново; `git log` говорит, что сделано, но не говорит, что дальше и какие пункты приёмки остались незакрытыми. Держать это в памяти агента отвергнуто: память переживает не всё и не видна человеку, а прогресс должен быть в репозитории рядом с кодом и уезжать на GitHub вместе с ним | — |
| 2026-09-03 | Промпты хранятся файлами `src/scout/prompts/{intent,l1,l2}/v{N}.md`, версия = имя файла, bump = новый файл + правка `PROMPT_VERSIONS` | Поле `prompt_version` в схемах ссылалось в пустоту: место хранения и порядок версионирования нигде не были заданы, из-за чего запрет 7 («не менять промпты автоматически») был неисполним — нечего было проверять на diff. Правка существующего файла вместо создания нового отвергнута: она задним числом ломает воспроизводимость прошлых прогонов | — |
| 2026-09-03 | Семейство `topic` сортируется по `stars`, а не по `best-match` | Правило сортировки и пример в `QUERIES.md` расходились. Выбран `stars`: в запросе из одних квалификаторов `topic:` нет свободного текста, `best-match` не с чем сопоставлять, порядок выдачи произволен и нестабилен между вызовами | — |
| 2026-09-03 | Пример q6 в `QUERIES.md` исправлен: `pdf tables` → `pdf table` (код не изобретает морфологию). `core_noun` — производная величина, которую считает генератор: первые два значимых слова из `synonyms[0]` | Поля `core_noun` в контракте `Intent` (`SCHEMAS.md` §2) нет, а шаблон `broad` его требует. Альтернатива — добавить поле в схему и бампнуть промпт до `intent-2` — отвергнута: это правка контракта и промпта ради величины, которая однозначно выводится из уже имеющегося синонима. Множественное число в примере кодом невоспроизводимо: морфологию ему взять неоткуда, а выдумывать её значит терять детерминизм | если golden-set покажет, что усечённый `broad` систематически промахивается |
| 2026-09-03 | `archived:false` добавлен в шаблоны и пример семейств `readme`, `broad`, `recent` | Текстовое правило `QUERIES.md` («во всех семействах, кроме `library`») и таблица шаблонов с примером расходились: в трёх строках квалификатора не было. Выбрано правило, а не пример — оно нормативно, а пример иллюстративен; заодно оно согласовано с жёстким отсечением `archived: true` на шаге дедупа, которое иначе делало бы ту же работу позже и дороже | — |
| 2026-09-03 | Пример запросов в `QUERIES.md` снимается с вывода генератора и сверяется тестом `test_queries_md_example_block_matches_generator` | Пример расходился с собственным документом дважды подряд, и оба раза это нашлось только вычиткой глазами. Держать в доке блок, который никто не проверяет, — заводить второй источник истины рядом с кодом | — |
| 2026-09-03 | История git всегда зелёная: тесты пишутся первыми в работе, но красный коммит в историю не уходит. `pytest` остаётся в pre-commit, `--no-verify` запрещён без исключений, в том числе для красных коммитов | Красный коммит ломает `git bisect`: тот ищет первый коммит, где тест падает, и заведомо падающая ревизия делает результат бессмысленным. Исключение «только для красных коммитов» размывает сам запрет `--no-verify` — он держится ровно на том, что исключений нет. TDD от этого не страдает: порядок «сначала тест, увидеть красное, потом код» соблюдается в работе, а история фиксирует результат, а не процесс | — |
| 2026-09-03 | `config.py` подгружает `.env` из корня проекта сам, своим парсером на 15 строк, при импорте модуля. Уже заданная переменная окружения не перезаписывается | `README.md` обещал «`cp .env.example .env`, затем вписать ключи», но код читал только `os.environ` — инструкция не работала, и живой прогон упёрся бы в «ключ не задан». Альтернатива «объяснить в README про `set -a; source .env`» отвергнута: костыль в терминале, который надо повторять в каждом новом окне и о котором забудут. Зависимость `python-dotenv` отвергнута ради пятнадцати строк разбора `KEY=value` | — |
| 2026-09-03 | `minItems: 5` в `SCHEMAS.md` §3 не снижается; вместо этого исправлена фраза `QUERIES.md` о наборе меньше пяти запросов | Снижать контракт под вырожденный случай, которого на живых интентах ни разу не наблюдалось, — менять гарантию ради гипотезы. Генератор добирает недостающее широким неводом, а неустранимую бедность интента честнее считать ошибкой, чем отправлять на поиск заведомо слабый набор | если golden-set покажет систематически бедные интенты |
| 2026-09-04 | Схлопывание форков двухступенчатое: по `parent.id`, а при его отсутствии в данных — по совпадению короткого имени с не-форком, у которого звёзд не меньше | `QUERIES.md` шаг 3 предписывает схлопывать по `parent.id`, но в элементах выдачи Search API поля `parent` нет вовсе — оно приходит только из `/repos/{full_name}`. Правило было неисполнимо на тех данных, к которым применяется. Альтернатива «дозапрашивать `/repos` на каждый результат» отвергнута: до 210 результатов до дедупа, это 210 вызовов `core` ради схлопывания единиц, при бюджете 5000 в час. Ограничение по звёздам отсекает однофамильцев: форк популярнее родителя — редкость, два несвязанных проекта с одинаковым именем — нет. Известный `parent.id`, которого нет в выдаче, эвристику не включает: там правило уже дало ответ | при диагнозе «болит поиск»: если на golden-set обнаружатся несхлопнутые пары форк–родитель или ложные схлопывания однофамильцев |
| 2026-09-04 | `rank_candidates` возвращает промежуточный `RankedRepo`, а не готовый `Candidate`; `head_sha` подставляется в `to_candidate` только для среза, идущего на Слой 1 | `Candidate` (`SCHEMAS.md` §4) требует `head_sha`, а поиск головы ветки не отдаёт — нужен отдельный вызов `/repos/.../commits/{branch}` на репозиторий. Собирать `Candidate` сразу означало бы делать этот вызов для всех результатов поиска вместо пятидесяти отобранных, то есть тратить `core` на кандидатов, которые отсеются сортировкой. Разделение заодно оставляет переранжирование чистой функцией без сети — критерий приёмки дня 4 проверяется без ключей | — |
| 2026-09-04 | В `Candidate.rrf_score` пишется нормализованный RRF (0…1), сырая сумма остаётся в `RankedRepo.rrf_raw` для логов | Поля итогового `score` в контракте §4 нет — порядок фиксирует `rank`. При фиксированных весах `score = 0,70 · rrf_score + 0,30 · prior_score` восстанавливается из записанных полей, но только если `rrf_score` нормализован: сырая сумма без максимума по выдаче невосстановима, и по архивной записи уже нельзя проверить, тем ли кодом получен порядок. Добавление поля `score` в схему отвергнуто: правка контракта ради величины, которая из него выводится | если веса перестанут быть константами модуля |
| 2026-09-04 | Peak-часы действуют только Пн–Пт: `config.is_peak()` проверяет `weekday()`, формулировки в `CLAUDE.md` и `ARCHITECTURE.md` приведены к прайсу | Официальный прайс DeepSeek ограничивает peak буднями («Monday through Friday»), а код и документация знали только часы. Цена вылезала боком дважды: счёт по выходным завышался ровно вдвое, и `--off-peak`, запущенный в субботу в 02:00, откладывал задачу на два часа, хотя дешевле уже не станет. Найдено при сверке прайса на дне 7, до попадания в живой замер: прогон пришёлся на четверг. День недели берётся по UTC вместе с часом — в Актобе (UTC+5) суббота начинается в 19:00 пятницы, и по местному календарю граница выходных сдвинулась бы на пять часов | ежемесячная сверка прайса вместе с ценами |
| 2026-09-04 | Правка таблицы цен в `ARCHITECTURE.md` разрешена как исключение из «документацию дня не трогать»: добавлены цены входа по cache hit для `deepseek-v4-flash` — 0,007 / 0,014 за 1M | Без них учёт стоимости дня 7 неисполним: в таблице цена кэша стояла только у V4-Pro, а Слой 1 работает на Flash, и на живом прогоне из кэша пришла половина входа (12 544 из 24 822). Альтернатива «считать кэш по полной цене входа» отвергнута числом: у Flash кэш дешевле в 31 раз, и по замеру дня 5 полная цена дала бы 0,0141 $ вместо 0,0114 $ — завышение на 24% при требуемой точности ±5%. Цены сверены с прайсом DeepSeek 2026-09-04; вход и выход в таблице совпали с прежними, менять их не пришлось | ежемесячная сверка прайса, как и для остальных строк таблицы |
| 2026-09-04 | Закрытые недели архивируются в `HISTORY.md`; `PROGRESS.md` держит только текущую неделю и технический долг. Лимит 200 строк остаётся на обоих файлах, кроме растущего по построению `HISTORY.md` | `PROGRESS.md` упирался в лимит каждые два-три дня, и каждый раз приходилось сжимать историю — из разделов дней 1–4 так ушли правила сборки запросов, список отсечек и разбор расхождений с документами. Сжатие теряло ровно те детали, ради которых файл и ведётся: следующая сессия читает его первым и не имеет другого способа узнать, почему сделано именно так. Поднять лимит отвергнуто: файл, который читают первым при каждом возобновлении, обязан оставаться коротким, иначе его перестают читать целиком. Разделение на горячее (`PROGRESS`) и холодное (`HISTORY`) хранение снимает конфликт навсегда: одна неделя укладывается в полсотни строк, а архив растёт, не мешая чтению | — |
| 2026-09-04 | Цель стоимости скана поднята с 0,15 до 0,20 $ off-peak (peak — с 0,30 до 0,40 $). Правки внесены в `CLAUDE.md` и `CHECKLIST.md` | Замер Слоя 1 на живом прогоне дня 5 дал ~1,3k выходных токенов на репозиторий вместо заложенных ~100: основная часть уходит на рассуждения модели. Это свойство reasoning-моделей, а не наша расточительность, и снимается только отказом от рассуждений — то есть потерей качества скрининга, ради которого слой и существует. Резать нечего: сокращать 4000 символов README значит слепить Слой 1, а урезать 50 кандидатов — бить по recall, единственной метрике проекта. Расчётные 0,18 $ за скан при 5–6 запусках в месяц — около 500 тенге, для личного MVP приемлемо. Альтернатива «оставить недостижимую цель» отвергнута: цель, которую система заведомо не выполняет, перестаёт быть гейтом и начинает быть шумом в `CHECKLIST.md` | после живого замера Слоя 2 (день 12): тогда же пересчитывается вся таблица токеномики `ARCHITECTURE.md`, где цифры Слоя 1 сейчас заведомо занижены |
| 2026-09-04 | Слой 1 распараллеливается пулом `Semaphore(5)` | Скрининг идёт строго последовательно: 16 с на репозиторий, полсотни дают ~13 минут против цели «полный цикл ≤ 5 минут». Пять одновременных вызовов укладывают Слой 1 примерно в 3 минуты. Больше пяти брать незачем: упор придёт не в DeepSeek, а в `core`-лимит GitHub на чтение README, и рост параллелизма начнёт грозить вторичным лимитом, ради обхода которого пришлось бы усложнять клиент. Порядок результатов от параллелизма не зависит: `passed` пересчитывается кодом сортировкой по `(relevance, repo_id)` | если DeepSeek начнёт отдавать 429 на пяти потоках — снижать, а не наращивать |
| 2026-09-04 | Fallback Слоя 2 идёт через OpenRouter: ключ `OPENROUTER_API_KEY` (переменная `QWEN_API_KEY` переименована), модель `qwen/qwen3.8-max`. Запрос обязан нести явный `max_tokens` | Прямой доступ к Qwen требует отдельной регистрации в Alibaba Cloud со своим биллингом и верификацией — цена входа несоразмерна пути, который срабатывает только при 5xx от DeepSeek. OpenRouter даёт тот же ответ по уже имеющемуся ключу. Наценки нет: живая проверка вернула `cost` 0,000728 $ за 79 входных и 95 выходных токенов, то есть ровно 2,00 / 6,00 $ за 1M из таблицы `ARCHITECTURE.md`. Префикс `qwen/` — маршрутизация OpenRouter, в схемах и отчётах модель остаётся `qwen3.8-max` (`SCHEMAS.md`, enum `ModelName`). Про `max_tokens`: без него OpenRouter резервирует под ответ весь контекст модели (65536 токенов) и отвечает 402, даже когда реальный ответ — одна строка | если fallback начнёт срабатывать регулярно — сравнить с прямым доступом и посчитать, окупается ли отдельный аккаунт |
| 2026-09-04 | Порядок шагов дедупа оставлен как в `QUERIES.md`: схлопывание форков идёт до жёсткого отсечения. Следствие — архивный родитель уводит с собой живой форк | Перестановка шагов «отсечь, потом схлопывать» спасла бы живой форк архивного родителя, но это правка правила ранжирования по догадке, без замера. Влияние мало: `archived:false` стоит во всех семействах, кроме `library`, поэтому архивные репозитории почти не попадают в выдачу. Поведение зафиксировано тестом `test_archived_parent_takes_live_fork_down_with_it` — не как одобренное, а чтобы смена порядка не прошла молча | при диагнозе «болит поиск» — вместе с замером recall@5 до/после, как любая правка ранжирования |

| 2026-09-04 | `GitHubClient._request` ловит `HttpError` от транспорта и повторяет его как 5xx — backoff 1/4/16 с джиттером, затем `GitHubUnavailable`. У 5xx свой счётчик попыток, отдельный от вторичного лимита | Симметричная дыра к той, что уронила живой прогон дня 5 со стороны DeepSeek: обрыв связи проходил мимо `_request` голым исключением. Слой 1 читает README каждого из пятидесяти кандидатов пятью потоками — полсотни вызовов `core` подряд без единой осечки это удача, а не норма, и одна осечка роняла весь скан вместе с уже потраченными деньгами. Общий счётчик попыток на 5xx и вторичный лимит отвергнут: две беды подряд — не одна беда, и второй заслуживает своих трёх попыток, а не остатка от чужих | — |
| 2026-09-04 | 401 и 403 «Bad credentials» выделены в `GitHubAuth` / `DeepSeekAuth` и не глушатся ни пулом Слоя 1, ни обработчиком отдельной выдачи в `search.py`. Код возврата — 3, как у отсутствующего ключа | Отклонённый ключ одинаков для всех пятидесяти кандидатов и всех семи запросов. Пойманный «как обычный отказ» он дал бы пустой скан с рекомендацией BUILD — то есть содержательный ответ «открытого решения не нашлось» там, где на самом деле не отправлено ни одного запроса. Это худший вид ошибки: неверный ответ вместо честного отказа. У 403 два разных смысла — лимит и отказ в доступе, — поэтому «Bad credentials» отличается по телу ответа, а не по статусу | — |
| 2026-09-04 | Коды возврата CLI оставлены дробными (2–7 по типу отказа) вместо предложенной пользователем схемы 0/1/2. Частичный результат — 0, непредвиденный сбой — 1, ошибка конфигурации — 3 | Предложенная схема отображала «нет результатов» в 1, но пустой поиск у нас не отказ, а содержательный ответ BUILD (`ARCHITECTURE.md`, последняя строка таблицы) — по ней он получил бы код провала. Дробная нумерация уже разведена по документам и тестам и несёт больше: по коду видно, чинить `.env` (3), ждать DeepSeek (4) или править запрос (2). Замысел ТЗ — «частичный успех не провал, конфигурация отличается от сбоя сервиса» — выполнен полностью, разошлись только числа. Схема выписана таблицей в докстринге `cli.py` | если появится внешний вызывающий скрипт, которому дробность мешает |
| 2026-09-04 | В `Report` (`SCHEMAS.md` §8) добавлено поле `partial`. Флаг поднимают только потери из-за сбоя; штатное выбытие кандидата даёт строку в `dropped`, но `partial` не трогает | Флаг требовался планом дня 8, а места в контракте у него не было — `SearchOutcome.partial` жил во внутренней структуре и дальше лога не уходил. Отложить до отчёта (день 13) значило бы менять контракт дважды. Разделение с `dropped` принципиально: репозиторий, удалённый между поиском и аудитом, не делает отчёт неполным — мы получили всё, что существовало, — а вот невыполненная выдача или неразобранный ответ модели делают, и молчать об этом нельзя. Смешать их значило бы поднимать флаг почти на каждом скане и тем обесценить его | — |
| 2026-09-04 | `_screen_one` ловит `Exception` целиком: неожиданный сбой на кандидате пишется как `screening_crash`, кандидат выбывает, пул продолжает. Наружу пропускаются только `MissingCredential`, `DeepSeekAuth`, `GitHubAuth` | `ThreadPoolExecutor.map` поднимает исключение потока в вызывающем коде — любая непредвиденная поломка на одном кандидате из пятидесяти уронила бы скан целиком, включая уже разобранных. Голый `except Exception` обычно порицаем, и здесь он оправдан ровно тем, что ошибка локализована: цена — один кандидат, который и так помечается `failed`. Три исключения из правила — те, что одинаковы для всех кандидатов сразу | — |
| 2026-09-04 | К паузе, названной GitHub в `Retry-After` / `X-RateLimit-Reset`, добавляется буфер 1 с | Повтор ровно в момент сброса иногда прилетает в тот же 403: часы GitHub и наши расходятся на секунды, а окно лимита закрывается по серверным. Секунда на фоне пауз в десятки секунд ничего не стоит, а лишняя потраченная попытка из трёх — стоит | — |
| 2026-09-04 | Фикстуры `ok_intent`, `offline` и помощник `screening_run` переехали в `tests/conftest.py`; сквозные проверки живучести собраны в `tests/test_errors.py` | Оба файла новые, поэтому запись здесь. `test_errors.py` нарушает конвенцию «тест на модуль» намеренно: он проверяет не модуль, а свойство всего конвейера — что отказ внутри шага остаётся отказом шага. Раскидать эти проверки по `test_github`, `test_screen` и `test_cli` значило бы потерять сам предмет проверки. `conftest.py` понадобился, потому что фикстуры делят два файла: импорт фикстуры из соседнего теста pytest допускает, но имя тогда попадает в модуль дважды, и ruff справедливо ругается на переопределение | — |

| 2026-09-04 | Конвейер скана вынесен из `cli.py` в `pipeline.py`; CLI оставил себе печать и коды возврата. Новый файл `src/scout/pipeline.py` | У конвейера стало двое читателей: оболочка печатает результат человеку, прогон golden-set считает по тем же объектам recall. Пока код жил в CLI, второму пришлось бы либо разбирать stdout, либо держать свою копию конвейера — и копия неминуемо разошлась бы с оригиналом, а расхождение проявилось бы как «метрика меряет не то, что делает продукт». Обработка отказов осталась в CLI намеренно: у оболочки и у набора на один и тот же отказ разные ответы — код возврата против строки в таблице | — |
| 2026-09-04 | Прогон golden-set считает два среза recall сразу: `recall@50` (нашёл ли поиск) и `recall@10` (дожил ли эталон до конца Слоя 1). Новый файл `src/scout/evaluate.py` | Одного числа мало для решения, которым заканчивается неделя 2. Низкий `recall@10` может означать две разные починки: поиск не нашёл эталон вовсе (`QUERIES.md`, веса, шаблоны) или нашёл, но Слой 1 отбросил (промпт `l1-1`). Без второго среза они неразличимы, и день 10 закончился бы гаданием. `recall@50` в `ROADMAP.md` уже назван вспомогательным, здесь он становится обязательным | — |
| 2026-09-04 | Задачи-ловушки (`expected_repos: []`) считаются инверсно: 1.0 за пустой `passed`, 0.0 за непустой | Обычная формула на пустом эталоне делит на ноль, а выбросить ловушки из набора значит потерять единственную защиту от вырождения: агент, который на любой запрос отдаёт пятёрку кандидатов, без ловушек получает то же recall, что и честный. Пока Слоя 2 нет, «рекомендация BUILD» операционально равна «никто не прошёл Слой 1» | когда появится Слой 2 — сверять с `Report.recommendation`, а не с пустотой `passed` |
| 2026-09-04 | Упавшая задача не входит в среднее: `cases_failed` считается отдельно от `recall`. У `scout eval` есть `--dry-run` (проверка набора без единого вызова модели) и `--limit N` (пилотный прогон первых N задач) | Обрыв сети на одной задаче иначе занижал бы recall и выглядел бы как плохое качество поиска — та самая подмена диагноза, ради которой делался день 8. `--dry-run` отделяет ошибку в данных от результата замера: опечатку в JSON нельзя ловить деньгами, набор из 25 задач стоит около 1,4 $ за прогон. `--limit` нужен пилоту: сначала за ~0,12 $ проверяется, что харнесс считает то, что надо, и только потом тратится полный бюджет | — |

| 2026-09-05 | OR-список семейства `library` обрезается до шести имён (пять операторов). Правило добавлено в `QUERIES.md` | Search API отвечает 422 «More than five AND / OR / NOT operators were used», и генератор об этом не знал: он резал список только по длине строки в 256 символов. Интент с семью гипотезами давал шесть `OR` — GitHub отклонял запрос целиком. Потеря была молчаливой и самой дорогой из возможных: `library` — единственное семейство, которое находит нишевые проекты по имени, а не по тексту, и отваливалось оно как раз на нишевых задачах, где нужнее всего. Найдено пилотом golden-set: 422 на двух задачах из восьми | — |
| 2026-09-05 | Неразбираемый ответ модели выделен в `DeepSeekBadResponse` и лечится повтором наравне с ответом не по схеме — и на Слое 1, и на извлечении интента | Пилот: на одном кандидате из ста двадцати модель вернула пустую строку вместо JSON. Живучесть дня 8 отработала штатно — пул выжил, — но кандидат выбыл насовсем, хотя стоил одного повтора. Для кандидата «пустая строка» и «JSON не по схеме» — одно и то же: ответа нет; лечение обязано быть одинаковым, а до этого разным было. На интенте цена ошибки выше: та же осечка стоила бы всего скана | — |
| 2026-09-05 | Задачи-ловушки исключены из `recall@50`: у них нет эталона, а поиск обязан что-то вернуть | Пилот дал recall@10 = 0,60 при recall@50 = 0,48 — потолок ниже метрики, которую он ограничивает. Это невозможно по построению: эталон, доживший до конца Слоя 1, обязан был сначала найтись поиском. Причина оказалась в метрике, а не в конвейере: ловушке ставился ноль за то, что поиск вернул кандидатов, — то есть поиск наказывался за правильную работу. Инвариант «recall@50 ≥ recall@10» закреплён тестом | — |

| 2026-09-09 | Эталоном golden-set не может быть репозиторий, который выбросит наш собственный отсев: форк, архив или репозиторий без кода. Набор доведён до 25 задач, границы состава закреплены тестами | Иначе задача измеряет не качество поиска, а нашу же настройку, и recall на ней ноль при полностью исправном конвейере. Проверка живыми запросами отменила два эталона сразу: `google/dagger` — форк архивного `square/dagger` (запрет 4 `CLAUDE.md` отсекает форки), `FasterXML/jackson` — зонтичный репозиторий с документацией и без кода. Альтернатива «сделать исключение в отсеве ради эталона» отвергнута: это подгонка продукта под метрику, ровно наоборот к тому, зачем метрика заводится. Ограничение продукта, которое при этом вскрылось, записано отдельно в `PROGRESS.md` → «Открытые проблемы» | когда отсев научится отличать форк-который-канон от обычного форка — пересобрать задачу про DI |
| 2026-09-09 | Поле `expected_verdict` в `tests/golden/*.json` (USE/FORK/BUILD) — заготовка для проверки Слоя 2 (дни 11–12). Сейчас оно не проверяется ничем: харнесс дней 9–10 доводит прогон только до Слоя 1, а тот отвечает `pass`/`reject`, а не вердиктом. **Не удалять как мусор:** после дня 12 в `evaluate.py` добавится сверка реального вердикта с ожидаемым | Поле выглядит мёртвым ровно до появления Слоя 2, и в этом окне его легко счесть недосмотром. Но эталонный вердикт — это разметка, которую делает человек, а не код: если удалить её сейчас, то к моменту, когда метрика точности вердиктов станет возможной, размечать придётся заново все двадцать пять задач. Цена хранения — одно поле в JSON, цена удаления — повторная ручная разметка набора | когда появится Слой 2 — спроектировать метрику точности вердиктов как расширение `evaluate.py` |
| 2026-09-09 | Задачи набора имеют разное число эталонов (1, 2 или 3), и вклад задачи в средний `recall` от этого зависит: потеря одного эталона из трёх снижает среднее слабее, чем потеря единственного. Это свойство набора, а не дефект метрики — задача с одним эталоном даёт бинарный вклад (0 или 1), задача с тремя — дробный | Зафиксировано при вычитке первых пяти задач. Без этой оговорки чтение `eval/{дата}.json` создаёт иллюзию равнозначности задач: полный провал `rust-cli-args` и потеря одного эталона из трёх в `ocr-receipts-offline` выглядят в столбце `recall_at_10` несопоставимо, хотя обе строки — просто число. Выравнивать веса отвергнуто: разброс по числу эталонов заложен составом набора намеренно, и нормировка скрыла бы именно то, что задачи разной строгости | после первого полного прогона — при разборе результатов по задачам |
| 2026-09-09 | Прогон набора возобновляем: флаг `--journal PATH`, результат каждой задачи дописывается на диск сразу после её завершения (`append_journal`, с `fsync`), повторный запуск с тем же файлом переиспользует измеренное и платит только за недостающее. Упавшие задачи в журнал не пишутся | Полный прогон 25 × 50 убили на 15-й задаче из 25: 0,59 $ были потрачены, а `write_report` вызывается один раз в самом конце `evaluate()`, поэтому на диск не попало ничего. Метрики удалось поднять только из структурного лога stderr, и то не целиком — списки `passed` пересортированы по `(relevance, repo_id)`, а relevance прошедших кандидатов в лог не идёт, так что имена восстановлению не поддались. Альтернатива «писать отчёт целиком после каждой задачи» отвергнута: перезапись растущего JSON на каждом шаге даёт окно, в котором убитый процесс оставляет обрезанный файл вместо прошлого целого. Дописывание строкой такого окна не имеет, а оборванный хвост журнала пропускается при чтении | если появится Слой 2 и прогон станет дороже часа — рассмотреть журнал и для промежуточных результатов внутри задачи |
| 2026-09-09 | **Baseline недели 2 зафиксирован: `recall@10` = 0,7333 при `recall@50` = 0,8561** на полном прогоне 25 × 50 (`eval/2026-09-09.json`), 0,9815 $ за набор, 0,039 $ на скан, медиана 180 с. Цель `ROADMAP.md` ≥ 0,60 взята, поэтому неделя 3 начинается со Слоя 2, а не с починки `QUERIES.md` | Число получено на наборе, границы которого закреплены тестами, и на харнессе, проверенном двумя пилотами. Собрано из двух прогонов: 14 задач измерены 2026-09-09 в первой попытке, убитой на середине, 11 досчитаны в тот же день по журналу. Прогоны разнесены на два часа, интент побайтово не воспроизводится (техдолг), поэтому набор строго говоря измерен не одним срезом — но альтернатива, повторить 14 задач ради однородности, стоила бы ещё 0,59 $ и дала бы ровно тот же разброс модели между задачами внутри одного прогона. Сравнение «до и после» для правок промптов ведётся против этого файла | при первом же изменении промптов или весов — новый прогон и сравнение с этим числом |
| 2026-09-09 | Журналы прогонов (`eval/*.journal.jsonl`) в git не кладутся — добавлены в `.gitignore`. Артефакт прогона один: отчёт `eval/{дата}.json` | Журнал — черновик на случай убийства процесса, и его ценность кончается в момент, когда отчёт записан: все те же строки лежат в отчёте, только полнее. Плюс он ломает гейт: gitleaks ловит в компактном JSONL `generic-api-key` на списке **имён репозиториев** (`openapi-generators/openapi-python-client` рядом с `square/kotlinpoet` даёт энтропию 3,73), тогда как тот же список в отчёте с отступами правило не трогает. Альтернатива — заводить `.gitleaksignore` с отпечатком — отвергнута: отпечаток свой у каждого прогона, и список исключений рос бы на каждом замере, размывая гейт ради файла, который не нужен. Происхождение baseline от этого не теряется: задачи, поднятые из убитого прогона, отличимы в отчёте по пустому `passed` при ненулевом `recall_at_10` | если журнал понадобится как артефакт — класть рядом с отчётом в том же формате, что и отчёт |
| 2026-09-10 | Слой 2 разделён так: **модель судит, код решает**. Модель возвращает структуру, зависимости, паспорт, покрытие, риски и четыре частные оценки; `score.total`, `verdict` и `code_reuse_allowed` считает код, а `repo_id`, `head_sha`, `model`, `prompt_version`, `maintenance` и `provenance` он проставляет сам. Поля из ответа модели, попадающие в эту зону, отбрасываются. Новые файлы `src/scout/audit.py`, `src/scout/repo_reader.py`, `src/scout/prompts/l2/v1.md` | Задание дня 11 просило у промпта вердикт, но `SCHEMAS.md` §7 требует обратного: `total` «считается детерминированным кодом, не моделью», правила вердикта «применяются кодом после получения оценок». Выбран контракт, и причина сильнее формальной: вердикт, произнесённый моделью, невоспроизводим — два прогона на одном репозитории дадут разные USE/FORK, и замер recall перестанет отличать изменение данных от разброса модели, то есть обессмыслит весь гейт `CHECKLIST.md`. `code_reuse_allowed` отдельно забран у модели: на нём висит юридическая часть отчёта, и `SCHEMAS.md` §6 выписывает правило целиком, так что доверять его суждению незачем | если появится задача, где пороги 0.70/0.50 систематически врут, — менять их только с замером recall до/после |
| 2026-09-10 | `GitHubClient.get_tree` получил параметр `limit`, и `repo_reader` читает дерево целиком (`limit=None`), а под бюджет контекста режет сам | Обрезка до 300 записей стояла в клиенте и применялась до того, как кто-либо искал в дереве файлы. Живой прогон дня 11 поймал последствие на `ispras/dedoc`: каталог `dedoc/` сортируется раньше корневых `pyproject.toml`, `requirements.txt` и `setup.py`, срез попадал внутрь каталога, и аудит получал «манифеста нет» при трёх манифестах в корне — то есть пустое поле `dependencies` по причине, к репозиторию отношения не имеющей. Сети обрезка не экономила: это срез уже полученного ответа. Альтернатива «отдельный нерекурсивный запрос корня» отвергнута — лишний вызов `core` на каждый аудит ради данных, которые уже пришли | — |
| 2026-09-10 | **Спроектировано, не реализовано:** `fit.gaps` станет массивом объектов `{note, blocking}`, и `USE` будет требовать отсутствия **блокирующих** пробелов вместо отсутствия всяких. Правка ждёт дня 13: по `CHECKLIST.md` она требует замера recall@5 до/после, а мерить нечем, пока нет отчёта | Живой прогон дня 11 дал FORK пяти кандидатам из пяти, и я поспешно заключил, что `USE` недостижим; день 12 это опроверг — `go-feature-flag` получил `USE`. Точная формулировка слабее и всё равно требует правки: `USE` достаётся тому, к кому модель не нашла придирок, а не тому, у кого нет важных пробелов — на дне 12 `Flagsmith` с 0,90 и BSD проиграл его одному пробелу про корпоративные функции. `SCHEMAS.md` §7 допускает «`gaps` пуст **или тривиален**», но `gaps` объявлен плоским списком строк, и признака тривиальности в типе нет — любой критерий выходил бы угадыванием по длине строки. Альтернатива «велеть промпту писать только блокирующее» отвергнута: это тихо возвращает решение модели, она сама определяла бы, что достаточно важно для лишения `USE`, и невоспроизводимо — ровно то, от чего день 11 уходил. Шкала severity 0–1 вместо булева отвергнута тоже: она вводит второй порог, калибровать который не на чем, размеченных пробелов у нас нет. При булевом модель отвечает на факт («блокирует ли этот пробел задачу»), код остаётся единственным, кто знает пороги и вес лицензии | день 13: вместе с отчётом — `l2-2`, модель `Gap`, правка §7 и замер recall@5 до/после |
| 2026-09-10 | Таблица токеномики `ARCHITECTURE.md` заменена **замеренной**: полный скан 0,186 $ off-peak против расчётных 0,13 $, сравнение с наивным подходом пересчитано (0,876 $, каскад дешевле на 79%). Цель ≤ 0,20 $ исполнена, но остаётся 7% коридора | Замер Слоя 2 на десяти репозиториях (день 12) показал выход в 3,9 раза выше плана при входе 0,74 от плана; у Слоя 1 выход превышен в 5,8 раза. Причина общая: обе модели reasoning, и расчёт закладывал ~100 токенов на ответ Слоя 1 и ~1,5k на ответ Слоя 2, не считая рассуждения вовсе. Держать в документе расчётные числа рядом с замеренными отвергнуто как источник второй истины — план оставлен отдельной колонкой именно как история ошибки, а не как альтернативный ответ. Снижать цель стоимости тоже отвергнуто: коридор исполняется, а занижать его под факт значит терять сигнал о перерасходе | при любой правке промптов или бюджета контекста — пересчитывать до правки, а не после |
| 2026-09-10 | `fit.gaps` стал массивом объектов `{note, blocking}`, промпт бампнут до `l2-2`, `USE` требует отсутствия **блокирующих** пробелов. Пробел без признака считается блокирующим | Реализация решения, спроектированного в тот же день. Умолчание выбрано строгим намеренно: ошибка в сторону FORK стоит лишней осторожности, ошибка в сторону USE — рекомендации взять то, что задачу не решает. Промпту отдельно запрещено выражать через `blocking` качество репозитория: для этого есть четыре оценки, а «блокирует ли пробел задачу» — вопрос о факте. Инструкция судить пробел относительно задачи, а не идеального продукта, дана примером: «нет OCR» при запросе про парсинг PDF-таблиц не блокирует, при запросе про сканы чеков — блокирует. Порядок проверок в `decide_verdict` не тронут: лицензия по-прежнему решает раньше порогов | замер recall@5 и распределения вердиктов до/после — прогоны 2026-09-10 |
| 2026-09-10 | Отчёт собирается детерминированным кодом (`report.py`), рекомендация скана — вердикт первого кандидата, а не отдельное суждение модели. Ловушки в метрике меряются по `Report.recommendation` | Двух источников вердикта не заводим: `recommendation` и `candidates[0].verdict` разошлись бы при первой же правке порогов, и отчёт стал бы противоречить сам себе. Обещание из записи 2026-09-05 («когда появится Слой 2 — сверять с `Report.recommendation`, а не с пустотой `passed`») исполнено: прокси врал на `trap-sap-payroll-bank-export`, где привязки к SAP RFC прошли скрининг, а правильный отчёт всё равно вынес бы BUILD. Markdown сделан производной от JSON без единого числа сверх объекта — иначе появился бы второй источник истины | — |
| 2026-09-10 | Детектор регургитации (`regurgitation.py`) сравнивает **нормализованный** текст: пробелы схлопнуты, регистр опущен. Проверяются все строковые поля, включая прозу; отказ лечится повтором | Побайтовое сравнение пропускало бы ровно те случаи, ради которых детектор заводится: модель переносит строки и схлопывает отступы, и перенос абзаца документации перестал бы совпадать с оригиналом. Исключать прозаические поля (`verdict_rationale`, заметки о рисках) отвергнуто: закон не различает «процитировал код» и «процитировал документацию», а мы обещали отдавать только пересказ. Отказ приравнен к невалидной схеме, а не к сбою: у кандидата есть попытка пересказать своими словами, и только потом он выбывает | если повтор будет срабатывать чаще, чем на одном кандидате из десяти, — смотреть промпт, а не порог |
| 2026-09-11 | Замер `blocking` признан **неизолированным**: `l2-2` остаётся в репозитории, но как незакрытая правка, а не как принятое улучшение. Прирост recall@5 (0,7083 → 0,7361) объявлен шумом | Состав выдачи совпал побайтово у одной задачи из двенадцати: у `k8s-secrets-in-git` Слой 1 пропустил восьмерых против троих, у `trap-kz-tax-form-200` поиск вернул ноль кандидатов вместо одиннадцати. Ни поиск, ни скрининг правка Слоя 2 затрагивать не могла. Весь прирост собран из одной ловушки, которая «взялась» потому, что Слой 1 в этот раз никого не пропустил, — то есть по старому механизму, а не по новой мерке. Засчитать такой прирост значило бы принять правку на основании погоды. Однозначный эффект правки при этом виден и велик: блокирующих пробелов 154 → 28, доля USE 19% → 64% | после починки процедуры замера — повторить A/B на зафиксированной выдаче |
| 2026-09-11 | **Процедура A/B из `CHECKLIST.md` неисполнима, пока выдача не зафиксирована.** До следующего замера промптов Слоя 2 нужно подавать обоим плечам один и тот же `Candidate[]` | Разброс выше измеряемого слоя оказался больше измеряемого эффекта, и правило «ухудшение recall@5 — основание откатить» превращается в подбрасывание монеты: два прогона одного набора расходятся на составе выдачи, числе прошедших скрининг и даже на числе найденных кандидатов. Причина известна и записана в техдолге — интент не воспроизводится побайтово при `temperature = 0`. Альтернатива «усреднять по нескольким прогонам» отвергнута по цене: каждый прогон двенадцати задач со Слоем 2 стоит около доллара, и статистически значимое усреднение стоило бы десятки. Фиксация выдачи даёт то же самое бесплатно и меряет ровно тот слой, который правят | — |
| 2026-09-05 | Результаты прогонов `eval/{дата}.json` держатся в git (каталог убран из `.gitignore`) | `CHECKLIST.md` требует замера recall до и после любой правки промпта или весов — сравнивать не с чем, если прошлые прогоны живут во временном каталоге и исчезают вместе с сессией. Файлы маленькие, один прогон это единицы килобайт, и в отличие от кэша они не воспроизводятся: каждый стоил денег и своего часа | если каталог начнёт расти неприлично — оставлять только baseline недели |

## Отменённые решения

| Дата | Что отменено | Причина |
|---|---|---|
| 2026-09-03 | «Слой 0 — Радар»: ежедневное сканирование трендов GitHub, Hacker News, Reddit, RSS | Смешивал два продукта: push-обнаружение новизны и pull-поиск под конкретную задачу. В запросе «нужен парсер PDF-таблиц» тренды последней недели бесполезны. Плюс скрытая стоимость: GitHub API не отдаёт историю звёзд, «звёздная скорость» требует своей БД снапшотов и начинает работать через месяцы после старта |
| 2026-09-03 | Модуль самоулучшения (§2.6 старого ТЗ) | См. решение об автономных действиях. Дополнительно снимало противоречие: «автономный режим применяет изменения» против запрета менять промпты без ведома пользователя |
| 2026-09-03 | Telegram-бот и Slack-интеграция | Асинхронный канал доставки нужен только асинхронному режиму. Пока `--off-peak` — опция, CLI достаточно |
| 2026-09-03 | Тестовый запуск репозиториев в Docker | См. решение о работе только через API |
| 2026-09-03 | Граф зависимостей и генерация UML | Без исходников строятся только по манифестам и дают немногим больше, чем список зависимостей, который уже есть в `AuditResult` |
| 2026-09-03 | «Мониторинг и обновление моделей» как подсистема с бенчмарками | Фактический объём работы — прочитать ченджлог DeepSeek и Qwen раз в месяц и поправить одну таблицу в `ARCHITECTURE.md` |
| 2026-09-03 | Роадмап на 6 месяцев (MVP 1–2 мес., v1.0 +2 мес., v1.5 +1 мес., v2.0 +2 мес.) | Оценки были перевёрнуты: скрининг с кэшем и CLI — это недели, а не два месяца, тогда как «граф зависимостей + бизнес-сущности + UML + Docker-запуск» — четыре разных проекта, а не один этап на два месяца. Заменён планом на 3 недели с гейтом на данных |
| 2026-09-03 | Запрет «peak-часы 08:00–20:00 UTC» из старого CLAUDE.md | Фактически неверно. Реальные peak-часы DeepSeek: 01:00–04:00 и 06:00–10:00 UTC. Старое ТЗ (§3.3) указывало окна верно, а CLAUDE.md — нет, и агент читал именно его |
| 2026-09-03 | Строка токеномики «Слой 3: V4-Pro или Qwen 3.8 Max, 2,00 / 6,00» | Смешивала цены двух моделей в одной ячейке: 2,00/6,00 — прайс Qwen, у V4-Pro 0,66/1,98 off-peak и 1,32/3,96 peak. Расчёт стоимости на её основе был неверен. Слой 3 отменён, цены разнесены по строкам |
| 2026-09-03 | Формулировка «Двухслойная + опциональная верификация» при четырёх слоях в архитектуре | Нумерация разошлась между документами. Введена единственная сквозная: Слой 1 — скрининг, Слой 2 — аудит |
| 2026-09-03 | Mermaid-диаграмма старого ARCHITECTURE.md | Невалидный синтаксис (цепочки стрелок, `/` в идентификаторах узлов) — не рендерилась. Заменена рабочей |
| 2026-09-03 | «Документация — 6 файлов, у каждого одна сфера» | Уточнено: пользователь утвердил добавление `README.md` и `CHECKLIST.md`. Состав набора — в таблице `CLAUDE.md`, см. активную запись выше |
