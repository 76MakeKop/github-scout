# PROGRESS.md — где мы находимся

**Сфера:** текущее состояние работ и точка возобновления.
**Обновлено:** 2026-09-04.

Читать первым при возобновлении работы. План — в `ROADMAP.md`, этот файл говорит,
какая его часть уже закрыта и чем именно.

## Сделано

### Дни 1–2 — каркас, схемы, клиент GitHub (`4720b84`, `7895e13`)

Пакет `scout`, CLI на argparse, `config.py` (лимиты 10/50/10/5, peak-часы),
`log.py` (JSON-строки в stderr), `schemas.py` (все 9 контрактов `SCHEMAS.md`,
`extra="forbid"`), `http.py` + `github.py` со всей таблицей ошибок из
`ARCHITECTURE.md`: `Retry-After`, вторичный лимит 1/4/16 с с джиттером,
троттлинг поиска 2 с, обрезка дерева до 300 путей. 39 тестов.

### Дни 2–3 — извлечение интента (`36ab6f1`)

`deepseek.py` (`chat_json`, `response_format=json_object`, бэкофф 1/4/16 с),
`intent.py` (`extract_intent` → обёртка `IntentExtraction`, один повтор
с текстом ошибки валидации), промпт `prompts/intent/v1.md`. Поля `request_id`,
`model`, `prompt_version` проставляет код, а не модель. 14 тестов.

### Неделя 1, день 3 — генератор запросов

- `queries.py`: семь семейств из `QUERIES.md`, детерминированная сборка строк.
  Момент времени — явный параметр `generated_at`, иначе окно `pushed:` семейства
  `recent` делало бы результат невоспроизводимым.
- Правила сборки — по таблицам `QUERIES.md`; отсев черновиков: дубли схлопываются,
  запрос длиннее 256 символов отбрасывается целиком, `id` нумеруются после отсева.
  Версия правил — `config.QUERY_GENERATOR_VERSION = "qg-1"`.
- `tests/test_queries.py`: 32 теста. Golden — `tests/fixtures/query_set_pdf.json`,
  написан вручную по примеру доки, а не снят с вывода кода. Отдельный тест читает
  блок примера прямо из `QUERIES.md` и сверяет с выводом генератора: этот пример
  расходился с собственным документом дважды, теперь молча разойтись не может.
- Интеграция в CLI: событие `queries_generated`, `QueryGenerationError` → код 6.
  Заглушка сдвинулась на `stage="search"`. `tests/test_cli.py`: 5 тестов на стык.

### Загрузка `.env`

`config.py` подгружает `.env` из корня проекта при импорте, своим парсером, без новой
зависимости: `README.md` обещал `cp .env.example .env`, а код читал только `os.environ`.
Уже заданная переменная окружения приоритетнее файла, отсутствие файла — не ошибка.
Тесты — в `tests/test_cli.py`, 6 штук.

### Неделя 1, день 4 — дедуп и переранжирование

- `rank.py`: дедуп по `repo_id`, схлопывание форков, жёсткое отсечение, RRF (`k = 60`),
  метаданный приор, сортировка по `(score DESC, repo_id ASC)`, топ-50. Момент времени —
  явный параметр `now`, по той же причине, что и `generated_at` у генератора запросов.
- Веса RRF и приора — константы модуля, а не аргументы: менять их разрешено только
  после замера recall@5 (`CLAUDE.md`, запрет 7).
- Три места, где выдача поиска не сходится с документами; все разобраны
  в `decisions_log.md` от 2026-09-04:
  `parent.id` в элементах Search API **нет** → схлопывание по имени с проверкой звёзд;
  `head_sha` нет → `rank_candidates` отдаёт промежуточный `RankedRepo`, а `head_sha`
  подставляется в `to_candidate` только для топ-50; поля итогового `score` нет
  в `SCHEMAS.md` §4 → `rrf_score` пишется нормализованным, чтобы скор восстанавливался
  из контракта.
- Порядок шагов оставлен по `QUERIES.md`: схлопывание форков раньше отсечения, поэтому
  архивный родитель уводит с собой живой форк. Влияние малое (`archived:false` стоит
  во всех семействах, кроме `library`), поведение закрыто тестом — чтобы смена порядка
  не прошла молча.
- `tests/test_rank.py`: 43 теста. Числа RRF и приора посчитаны вручную по формулам
  `QUERIES.md`, а не сняты с вывода кода. Критерий приёмки дня 4 закрыт: юнит-тест RRF
  на синтетических списках с известным ответом.

**Итого: 137 тестов, `ruff check` и `ruff format` чисто.**

### Живой прогон интента — критерий приёмки дней 2–3 закрыт

Ключ оплачен и вписан 2026-09-03. Прогон на 5 задачах: PDF-таблицы на Python,
офлайн-ASR для русского, HTML→PDF в Node.js, миграции PostgreSQL, DICOM на Rust.
**Все пять разобраны с первой попытки**, повторов не потребовалось.

Гипотезы имён библиотек оказались сильной стороной, как и рассчитывал `QUERIES.md`:
`vosk`/`whisper`/`faster-whisper` для русского ASR, `flyway`/`liquibase`/`alembic`/`goose`
для миграций, `dicom-rs` для Rust — всё реальные профильные проекты.

Подтверждено на платформе: модели называются ровно `deepseek-v4-flash` и
`deepseek-v4-pro` (плюс `deepseek-v4-flash-vision-exp`), как записано в `CLAUDE.md`.
Кэш промпта работает: со второго вызова `cached_input_tokens` = 1280 из ~1378.

Стоимость: вход 6888, выход 5807 токенов на 5 задач → **0,0011 $ на интент** off-peak.
В `ARCHITECTURE.md` на этап «интент + генерация запросов» заложено 0,0006 $ —
занижено вдвое, потому что модель отдаёт ~1,2k выходных токенов вместо 0,5k.
На цель «≤ 0,15 $ за скан» не влияет, но строку в токеномике стоит поправить,
когда появятся замеры Слоёв 1 и 2.

### Ключи на месте, лимиты проверены (2026-09-04)

Три ключа в `.env`, значения проверены на мусор в хвостах — чисто.
`GITHUB_TOKEN` — fine-grained, только чтение публичных репозиториев.
`OPENROUTER_API_KEY` — fallback Слоя 2, живой ответ `qwen/qwen3.8-max` получен.
Кода переключения нет, это день 14; про обязательный `max_tokens` — в `decisions_log.md`.

Живой `/rate_limit` с этим токеном:

| Ресурс | С токеном | Аноним |
|---|---|---|
| `search` | 30 запросов **в минуту** | 10 в минуту |
| `core` | 5000 **в час** | 60 в час |

Окна определены по времени сброса (`search` — 60 с, `core` — 3600 с), а не на глаз:
в выдаче GitHub оба поля выглядят одинаково и их легко перепутать.
Скан шлёт 7 поисковых запросов с паузой 2 с — запас четырёхкратный.
Важнее `core`: со дня 5 Слой 1 пойдёт читать метаданные и README 50 кандидатов,
и анонимных 60 запросов в час не хватило бы даже на один скан.

## Не закрыто в уже сделанном

- Ничего. Все критерии приёмки дней 1–4 закрыты.

## Следующий шаг

**Неделя 1, день 5: интеграция конвейера, затем Слой 1.**

Интеграции нет ни в одном дне `ROADMAP.md` — это дыра в плане, а не забытая работа:
между «запросы сгенерированы» (день 3) и «Слой 1 получает `Candidate[]`» (день 5)
никто не выполняет сами запросы. Делается в начале дня 5, до скрининга:

1. Прогон запросов через `GitHubClient.search_repositories` — троттлинг 2 с и потолок
   в 10 поисков уже внутри клиента.
2. Склейка выдач: `rank_candidates` → топ-50, событие `candidates_ranked` в лог.
3. `get_head_sha` на отобранные 50 → `to_candidate` → `Candidate[]`.
4. Вырожденные случаи `QUERIES.md` шага 5: меньше 10 уникальных кандидатов — добор
   широким неводом без `language:` и `stars:`; ноль кандидатов — отчёт с BUILD.
5. Заглушка сдвигается с `stage="search"` на `stage="screening"`.

Дальше — сам Слой 1: скрининг по метаданным и 4000 символов README, промпт `l1-1`.

Перед началом дня 5 стоит знать: `GitHubClient` умеет `search_repositories`,
`get_repo`, `get_tree`, `get_file`, `get_head_sha`; публичного метода для
`/rate_limit` нет — при разовой проверке дёргался приватный `_request`.
Если понадобится показывать остаток лимита в логах скана, метод придётся завести.

## Не запушено

Дни 1–3 запушены 2026-09-04, `origin/main` = `d589ebc`. Коммит дня 4 лежит локально.
Пуш делается только по явной просьбе — спросить при возобновлении.

## Три находки живого прогона — проверить на golden-set

1. **`core_noun` теряет предмет задачи.** Для «офлайн-ASR на русском» главный синоним —
   `offline russian speech recognition`, первые два значимых слова дают
   `offline russian`, и семейство `broad` ушло в поиск как
   `offline russian stars:>100 archived:false`. Существительное, ради которого всё
   затевалось, отброшено. Правило «первые два слова» работает, когда предмет стоит
   в начале (`pdf table extraction` → `pdf table`), и ломается, когда впереди
   определения. Кандидат на замену — брать два **последних** значимых слова либо
   самое длинное существительное. Менять только после замера recall@5: сейчас это
   догадка против догадки.
2. **`temperature = 0` не даёт побайтовой воспроизводимости.** Одна и та же задача
   в двух прогонах подряд дала 8 и 6 синонимов, разные формулировки, 2501 и 1556
   выходных токенов. `QUERIES.md` говорит «`temperature = 0`, версия промпта
   фиксируется» — воспроизводимость шага 1 этим не обеспечивается, и golden-set
   придётся мерить с усреднением по нескольким прогонам, а не по одному.
3. **`Node.js` → `languages: ["javascript"]`.** У GitHub `language:` — доминирующий
   язык репозитория, и заметная часть современных Node-библиотек помечена как
   TypeScript. Запрос с `language:javascript` их не увидит. Проверить на golden-set,
   не нужно ли для JS-задач опускать `language:` вовсе.

## Наблюдение с прошлого прогона — проверить на golden-set

При поиске «pdf table extraction language:python» эталонный `camelot-dev/camelot`
**не попал в топ-8** ни при сортировке по звёздам, ни при `best-match`. Нашёлся только
через семейство `library` (запрос по гипотезам имён библиотек), 5-я позиция.

Это первое эмпирическое подтверждение, что одного запроса мало и что семейство
`library` — не декоративное, а основное. Заодно там же в выдаче оказались
`camelot-dev/camelot` и форк `atlanhq/camelot` — ровно случай, ради которого
в `QUERIES.md` написано правило схлопывания форков.

Доки по этому поводу не правились: `CHECKLIST.md` требует замера recall@5 до/после,
а golden-set появится на дне 9. Проверить тогда.

## Известные несостыковки (зафиксированы, не исправлены)

1. **`scan "test"` из критерия приёмки дня 1 невозможен**: `SCHEMAS.md` §1 задаёт
   `query_text` минимум 8 символов. Рабочая проверка — `scan "test query"`.
   Развилка: снизить порог или поправить формулировку критерия в `ROADMAP.md`.
2. **Задание дней 2–3 требовало `Intent` с полем `status`**, которого нет в
   `SCHEMAS.md` §2 при `extra="forbid"`. Статус вынесен в обёртку `IntentExtraction`;
   контракт `Intent` не тронут.
3. **Необязательные поля схем** (`forks`, `open_issues`, `created_at`, `file_count`)
   в Python объявлены как `X | None = None`. Формально шире схемы: явный `null`
   пройдёт, хотя JSON Schema его запретила бы.
Несостыковка про «меньше 5 запросов» **снята**: `minItems: 5` оставлен, фраза
в `QUERIES.md` приведена к реальному поведению генератора (`decisions_log.md`).

## Окружение

- Системный Python 3.9.6 не годится (нужен 3.11+). Стоит 3.12.14 через `uv`:
  `pip3 install --user uv`, `uv python install 3.12`. Homebrew на машине нет.
- Работать через `.venv` проекта: `.venv/bin/pytest`, `.venv/bin/ruff`.
- **pre-commit требует venv в PATH:** `PATH="$PWD/.venv/bin:$PATH" git commit ...`,
  иначе хук pytest падает с «Executable pytest not found».
- `gh` авторизован под `76MakeKop`. Репозиторий приватный, локаль и origin — зеркало.
