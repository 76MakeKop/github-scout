# PROGRESS.md — где мы находимся

**Сфера:** текущая неделя и технический долг. Закрытые недели — в `HISTORY.md`.
**Обновлено:** 2026-09-10.

Читать первым при возобновлении работы. План — в `ROADMAP.md`, этот файл говорит,
какая его часть закрыта прямо сейчас и что делать следующим.

Файл держится коротким намеренно: как только неделя закрывается, она целиком
переезжает в `HISTORY.md` и отсюда исчезает. Здесь только горячее.

## Baseline недели 2 — измерен 2026-09-09

`25 задач × 50 кандидатов`, отчёт `eval/2026-09-09.json`.

| Показатель | Значение | Цель |
|---|---|---|
| **recall@10** | **0,7333** | ≥ 0,60 — **взята** |
| recall@50 (потолок поиска) | 0,8561 | — |
| Стоимость всего набора | 0,9815 $ | — |
| Стоимость одного скана | 0,039 $ | ≤ 0,20 $ — с запасом в 5 раз |
| Медиана времени задачи | 180 с | ≤ 5 мин — взята |
| Измерено / упало | 25 / 0 | — |
| Ловушек взято | 2 из 3 | — |
| Неполных прогонов | 1 (`elixir-background-jobs`) | — |

**Это то число, с которым сравнивают всё дальнейшее.** Неделя 3 начинается с аудита,
а не с починки поиска: условие `ROADMAP.md` «ниже 60% — чинить `QUERIES.md`» не
наступило.

### Разрыв 0,86 против 0,73 — где именно теряется

Тринадцать задач из двадцати двух неловушечных дали чистую единицу. Потери
разложились по диагностической таблице `ROADMAP.md` так:

**Болит поиск — эталона нет даже в топ-50 (6 задач).** `elixir-background-jobs`
теряет всё (`oban-bg/oban` не найден вовсе), `dotnet-orm` и `openapi-client-codegen`
по половине, `demand-forecast-timeseries` и `k8s-secrets-in-git` по трети.

**Болит скрининг — нашли, но выбыл на Слое 1 (4 задачи).** И здесь виден паттерн,
а не разброс: `russian-address-parsing` 1,00 → 0,00 и `kazakh-nlp` 0,50 → 0,00 —
**обе русскоязычные NLP-задачи, обе найдены поиском и обе выброшены скринингом.**
Два случая на одном классе задач — это уже система. `kotlin-android-http` и
`react-state-management` теряют по половине.

Суммарно скрининг съедает 0,12 из потолка. Это меньше, чем теряет поиск, но
концентрированнее: потери скрининга ложатся на не-англоязычные предметные области.

### Ловушка `trap-sap-payroll-bank-export` не сработала

Слой 1 пропустил пятерых: `jdorner/node-sapnwrfc`, `stonewm/sap_interface_pyrfc`,
`amarvin/pyrfc-read`, `jdsricardo/SAP-RFC-Python-without-PyRFC`, `redstar/sapnwrfc-d`.
Все пять — низкоуровневые привязки к SAP RFC. Задачу они не решают: это транспорт,
на котором решение можно было бы построить, а не выгрузка расчётных листков
в формат банка.

**Ловушка при этом валидна, а мерка — нет.** На момент замера Слоя 2 не было,
и «рекомендация BUILD»
операционально равна «никто не прошёл Слой 1» (`decisions_log.md`, 2026-09-05), и
этот прокси здесь врёт: правильный отчёт назвал бы эти пять строительными блоками
и всё равно вынес BUILD. Настоящая проверка ловушки появится вместе с
`Report.recommendation` на дне 13. До тех пор 2 из 3 — это нижняя оценка, а не факт.

Отдельно это вскрыло реальный дефект Слоя 1: он не отличает «готовое решение
задачи» от «библиотеки, на которой её можно построить». Для продукта, весь смысл
которого в вердикте USE/FORK/BUILD, это по существу, а не по метрике.

## День 11 — Слой 2 встал в конвейер

`repo_reader.py` собирает материал (дерево, до трёх манифестов, README, лицензия)
и укладывает его в бюджет 12k токенов, урезая в порядке «лицензия → дерево →
манифесты → README»: README режется последним, он единственный описывает замысел
проекта словами автора. Исходный код модулей не запрашивается вовсе — это
«Разделение контекстов», а не экономия.

`audit.py` — **модель судит, код решает.** Модель даёт четыре частные оценки,
структуру, зависимости, паспорт, покрытие и риски; `total`, вердикт и
`code_reuse_allowed` считает код, он же проставляет `repo_id`, `head_sha`,
`maintenance` и `provenance`. Поля модели, попавшие в эту зону, отбрасываются —
три теста на то, что подсунутый вердикт и `repo_id` игнорируются. Промпт
`prompts/l2/v1.md` говорит об этом прямо: «Judge the parts; the decision is not
yours to make».

**Техдолг про `--refresh` закрыт:** `--refresh` доходит до `AuditCache(CACHE_PATH, refresh=...)`.
Два теста — на попадание в кэш при той же ревизии и на его обход.

**393 теста, `ruff` чисто.** Новое: `repo_reader.py`, `audit.py`, `prompts/l2/v1.md`,
`tests/test_repo_reader.py`, `tests/test_audit.py`.

### Живой прогон: 5 аудитов за 5,3 цента

`scan "нужен парсер PDF-таблиц на Python" --max-candidates 15 --audit-limit 5`.
Слой 2 — **0,052602 $** (30 578 вход / 18 890 выход), весь скан 0,067686 $, ноль
повторов, ноль провалов. Материал уложился в 2,7–8,9k токенов при бюджете 12k,
урезать не пришлось ни разу.

**Токеномика Слоя 2 сошлась с расчётом, в отличие от Слоя 1:** 0,0105 $ на репо
против 0,0109 $ в `ARCHITECTURE.md`. Вход вдвое ниже заложенного, выход в 2,5 раза
выше — и они погасили друг друга.

**Прогон поймал два дефекта, оба починены:**

1. **Манифесты терялись за срезом дерева.** `GitHubClient.get_tree` резал дерево до
   300 записей до того, как в нём искали файлы. У `ispras/dedoc` каталог `dedoc/`
   сортируется раньше корневых `pyproject.toml`, `requirements.txt` и `setup.py`,
   срез попадал внутрь каталога — аудит получал «манифеста нет» при трёх манифестах
   в корне. Теперь `get_tree` принимает `limit`, а `repo_reader` читает дерево целиком
   и режет сам. Проверено живьём: `dedoc` нашёл три манифеста вместо нуля.
2. **`environment.yml` не считался манифестом**, а `microsoft/table-transformer`
   объявляет зависимости только в нём. Добавлены `environment.yml` и `setup.cfg`.

## Следующий шаг

День 12 по `ROADMAP.md`: лицензионный паспорт довести до живой проверки (AGPL-проект
обязан получить `code_reuse_allowed: false` и не стать `USE` — логика есть и покрыта
тестами, живого случая ещё не было) и замерить Слой 2 на десяти репозиториях ради
пересчёта токеномики (открытая проблема 1).

Перед этим — разобрать открытую проблему 4 ниже: сейчас вердикт `USE` недостижим.

**Договорённости по бюджету:** пилот перед полным прогоном обязателен, полный прогон
только в off-peak (выходные — off-peak целиком), `--dry-run` перед любой тратой,
`--journal` на любом прогоне дороже пилота.

## Открытые проблемы

1. **Токеномика Слоя 1 занижена втрое.** ~2,5k вход и ~1,3k выход на репозиторий
   против заложенных в `ARCHITECTURE.md` 75k/5k на все 50 (по факту ближе к 124k/66k).
   Причина в выходе: заложено ~100 токенов на ответ, а reasoning-модель тратит их
   на рассуждения. Цель стоимости уже поднята до 0,20 $, и baseline показал 0,039 $
   на скан — запас есть; пересчёт таблицы после живого замера Слоя 2, день 12.
2. **Отсев форков выбрасывает канонические проекты.** `google/dagger` — форк
   архивного `square/dagger`, и запрет 4 `CLAUDE.md` делает главный DI-фреймворк для
   Java и Android недостижимым навсегда. Признак есть: у такого форка родитель архивный
   и звёзд у родителя меньше — та же асимметрия, на которой стоит схлопывание форков.
   Правка отбора стоит замера recall до/после.
3. **Слой 1 не отличает решение от строительного блока.** Вскрыто ловушкой
   `trap-sap-payroll-bank-export`: пять привязок к SAP RFC прошли скрининг задачи,
   готового решения которой не существует. Чинится промптом `l1-2`, но проверить
   починку нечем, пока вердикт меряется пустотой `passed`, — то есть после дня 12.
4. **Вердикт `USE` сейчас недостижим.** Живой прогон дня 11: пять кандидатов из пяти
   получили `FORK`, включая `jsvine/pdfplumber` с `total` 0,96, лицензией MIT и
   задачей, которую он решает ровно. Причина в том, что `SCHEMAS.md` §7 требует для
   `USE` «`gaps` пуст **или тривиален**», а код проверяет только «пуст»: критерия
   тривиальности в контракте нет, и я его не выдумывал. Модель же честно пишет
   непустой `gaps` почти всегда — промпт сам просит не объявлять полное покрытие
   зря, и `pdfplumber` получил «no OCR for scanned PDFs», хотя OCR в задаче не
   спрашивали. Правка затрагивает и схему, и промпт: нужен признак блокирующего
   пробела против косметического (`fit.gaps[].blocking`), чтобы код отличал одно
   от другого, — и по `CHECKLIST.md` это требует замера recall@5 до/после, то есть
   ждёт отчёта дня 13. До тех пор распределение вердиктов читать нельзя.
5. **Скрининг систематически теряет не-англоязычные предметные области.**
   `russian-address-parsing` и `kazakh-nlp`: оба эталона найдены поиском, оба выброшены
   Слоем 1. Смотреть `red_flags` в `candidate_rejected` этих двух прогонов прежде,
   чем трогать промпт.

## Технический долг

1. **`scan "test"` из критерия приёмки дня 1 невозможен**: `SCHEMAS.md` §1 задаёт
   `query_text` минимум 8 символов. Рабочая проверка — `scan "test query"`.
   Развилка: снизить порог или поправить формулировку критерия в `ROADMAP.md`.
2. **Задание дней 2–3 требовало `Intent` с полем `status`**, которого нет в `SCHEMAS.md`
   §2 при `extra="forbid"`. Статус вынесен в `IntentExtraction`, контракт не тронут.
3. **Необязательные поля схем** (`forks`, `open_issues`, `created_at`, `file_count`)
   в Python — `X | None = None`: формально шире схемы, явный `null` пройдёт.
4. **Находки живых прогонов, ещё не закрытые baseline'ом**: усечение `core_noun`,
   отсутствие побайтовой воспроизводимости при `temperature = 0`. Подробности —
   `HISTORY.md`. Закрыты baseline'ом: `camelot-dev/camelot` вернулся в топ
   (`pdf-tables` = 1,00), `language:javascript` против TypeScript не подтвердился —
   `zustand` и `redux-toolkit` оба найдены поиском.
5. **`browser-automation-node` проходит по хрупкой причине.** Задача должна проверять
   находку дня 5 про `language:javascript`, а проходит благодаря семейству `library`.
   Если интент не извлечёт «puppeteer» и «playwright» в `known_libraries`, остальные
   семейства могут пропустить TypeScript-репозитории. Проверку теперь дублирует
   `react-state-management`, где оба эталона тоже на TypeScript.
6. **У 14 задач baseline нет списков `passed`.** Это те, что уцелели после убитого
   прогона: метрики записаны точно, имена прошедших кандидатов невосстановимы.
   Диагностика по ним беднее — видно, сколько эталонов дожило до топ-10, но не видно,
   какие именно и кто их вытеснил. Досчитать можно повторным прогоном за ~0,59 $;
   на само число baseline не влияет.
7. **Интент сужается до узкого существительного.** «Сканы чеков» дали поиск про чеки,
   а не про OCR. Правка промпта требует замера recall до/после — теперь возможна.

## Ключи и лимиты

Три ключа в `.env`, хвосты чистые. `GITHUB_TOKEN` — fine-grained, чтение публичных
репозиториев. `OPENROUTER_API_KEY` — fallback Слоя 2: ответ `qwen/qwen3.8-max` получен,
но API **не оплачен, до оплаты не задействовать**; кода переключения нет (день 14),
про обязательный `max_tokens` — в `decisions_log.md`. Живой `/rate_limit`: `search` —
30 в минуту, `core` — 5000 в час; важнее `core`, Слой 1 читает README каждого кандидата.
Публичного метода для `/rate_limit` у `GitHubClient` нет — дёргается `_request`.

## Окружение

- Системный Python 3.9.6 не годится (нужен 3.11+). Стоит 3.12.14 через `uv`:
  `pip3 install --user uv`, `uv python install 3.12`. Homebrew на машине нет.
- Работать через `.venv` проекта: `.venv/bin/pytest`, `.venv/bin/ruff`.
- **pre-commit требует venv в PATH:** `PATH="$PWD/.venv/bin:$PATH" git commit ...`,
  иначе хук pytest падает с «Executable pytest not found».
- `gh` авторизован под `76MakeKop`. Репозиторий приватный, локаль и origin — зеркало.
