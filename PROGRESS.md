# PROGRESS.md — где мы находимся

**Сфера:** текущая неделя и технический долг. Закрытые недели — в `HISTORY.md`.
**Обновлено:** 2026-09-04.

Читать первым при возобновлении работы. План — в `ROADMAP.md`, этот файл говорит,
какая его часть закрыта прямо сейчас и что делать следующим.

Файл держится коротким намеренно: как только неделя закрывается, она целиком
переезжает в `HISTORY.md` и отсюда исчезает. Здесь остаётся только горячее.

## Неделя 2 — сделано

### Слой 1 распараллелен (`e72fa8b`)

Кандидаты разбираются пулом из пяти потоков (`SCREENING_WORKERS`): последовательно
выходило ~16 с на репозиторий и ~13 минут на полсотни, пятеро одновременно дают ~3.
Потолок в пять — упор придёт не в DeepSeek, а в `core`-лимит GitHub (`decisions_log.md`).
Порядок результатов не плывёт: `map` отдаёт их в порядке входа, токены складываются
после пула в одном потоке. Под замок убраны запись в лог (пять потоков переслаивали бы
строки JSON) и счётчик вызовов `DeepSeekClient`.

### Лог отказов и цель стоимости (`f3b80c1`)

Событие `candidate_rejected` с `red_flags` и причинами: без него на дне 10 не отличить
потерю на поиске от потери на скрининге. Цель стоимости скана поднята с 0,15 до 0,20 $
off-peak (peak — до 0,40 $) во всех трёх местах, где живёт число: `CLAUDE.md`,
`CHECKLIST.md`, здесь. Причина — в `decisions_log.md`.

### День 6 — кэш аудитов, SQLite (`2ce7a6a`)

- `cache.py`: строка на `repo:{repo_id}:{head_sha}`, `AuditResult` в JSON. Инвалидация
  только по SHA — новый коммит даёт новый ключ и промах; старые строки живут дальше,
  `hits` покажут востребованность. TTL нет.
- `AuditCache`: `get`/`get_entry` (растит `hits` ровно в одном месте), `put`
  (`INSERT OR REPLACE`), `entries`, `drop`, контекстный менеджер. Соединение сразу
  потокобезопасное (`check_same_thread=False` плюс замок): Слой 2 пойдёт по кандидатам
  параллельно, как уже ходит Слой 1.
- `lookup_or_store` — политика «сначала кэш» с событиями `cache_hit` и `cache_miss`.
  Живёт в `cache.py`, а не в слое: Слоя 2 ещё нет, а приёмку показать надо. День 11
  передаст сюда вызов модели как `produce`.
- `scout cache list` и `scout cache drop` вместо заглушки — они в критерии приёмки
  дня 6 и в `SCHEMAS.md` §9 как способ чистки при смене `payload_type`.
- Кэшируется только `AuditResult`; `ScreeningResult` — нет, он дёшев и после
  распараллеливания быстр. `.cache/`, `*.sqlite3-wal`, `*.sqlite3-shm` в `.gitignore`.
- 23 теста: round-trip всего контракта §7, hit/miss, счёт `hits`, инвалидация по SHA,
  три режима `--refresh`, «второй вызов с тем же ключом = 0 вызовов модели», list/drop.
  Живой замер отложен до дня 12: кэшировать пока нечего.

**Итого: 215 тестов, `ruff check` и `ruff format` чисто.**

## Следующий шаг

**День 7: учёт стоимости и режим `--off-peak`** (`ROADMAP.md`).

1. Подсчёт `cost_usd` по таблице `ARCHITECTURE.md`, peak/off-peak строго по UTC.
   Сейчас поле стоит нулём во всех `TokenUsage` — заполнить.
2. Считать с учётом cache hit: `cached_input_tokens` по цене кэша, остальной вход —
   по полной. Цену кэша для V4-Flash предстоит внести в таблицу `ARCHITECTURE.md`
   из прайса DeepSeek: её там нет, а на живом прогоне больше половины входа Слоя 1
   пришло из кэша. Решение пользователя — правку сделать, зафиксировав в `decisions_log.md`.
3. Режим `--off-peak`: постановка в очередь и выполнение в ближайшее окно.

Критерии приёмки: цена в отчёте сходится с ручным расчётом ±5%; задача,
поставленная в 02:00 UTC, стартует в 04:00 UTC.

## Открытые проблемы

1. **Токеномика Слоя 1 занижена втрое.** Замер на 10 кандидатах: 24 822 входных
   (12 544 из кэша) и 13 139 выходных токенов — ~2,5k вход и ~1,3k выход
   на репозиторий. В `ARCHITECTURE.md` заложено 75k/5k на все 50, по факту ближе
   к 124k/66k. Причина в выходе: заложено ~100 токенов на ответ, а модель тратит
   их на рассуждения. Это свойство reasoning-моделей, а не расточительность:
   цель стоимости уже поднята до 0,20 $, пересчёт всей таблицы токеномики —
   после живого замера Слоя 2, день 12.

## Технический долг

1. **Полного прогона на 50 кандидатах не было** — живая проверка Слоя 1 сделана
   на `--max-candidates 10`. Остальные критерии приёмки дней 1–6 закрыты.
2. **`scan "test"` из критерия приёмки дня 1 невозможен**: `SCHEMAS.md` §1 задаёт
   `query_text` минимум 8 символов. Рабочая проверка — `scan "test query"`.
   Развилка: снизить порог или поправить формулировку критерия в `ROADMAP.md`.
3. **Задание дней 2–3 требовало `Intent` с полем `status`**, которого нет в
   `SCHEMAS.md` §2 при `extra="forbid"`. Статус вынесен в обёртку `IntentExtraction`;
   контракт `Intent` не тронут.
4. **Необязательные поля схем** (`forks`, `open_issues`, `created_at`, `file_count`)
   в Python объявлены как `X | None = None`. Формально шире схемы: явный `null`
   пройдёт, хотя JSON Schema его запретила бы.
5. **`--refresh` — рабочий флаг без потребителя.** Разобран в `ScanOptions`, доходит
   до `cmd_scan`, но передать его некуда: `scan` не открывает кэш, потому что
   кэшируется только `AuditResult`, а Слоя 2 нет. Режим протестирован на уровне кэша
   (три теста), проводка — на дне 11 одной строкой
   `AuditCache(refresh=request.options.refresh)`.
6. **Четыре находки живых прогонов ждут golden-set** (дни 9–10): усечение `core_noun`,
   отсутствие побайтовой воспроизводимости при `temperature = 0`, `language:javascript`
   против Node-библиотек на TypeScript, `camelot-dev/camelot` вне топа.
   Подробности — `HISTORY.md`, неделя 1.

## Ключи и лимиты

Три ключа в `.env`, хвосты чистые. `GITHUB_TOKEN` — fine-grained, чтение публичных
репозиториев. `OPENROUTER_API_KEY` — fallback Слоя 2: ответ `qwen/qwen3.8-max` получен,
но API **не оплачен, до оплаты не задействовать**; кода переключения нет (день 14),
про обязательный `max_tokens` — в `decisions_log.md`.

Живой `/rate_limit`: `search` — 30 запросов в минуту против 10 анонимных, `core` —
5000 в час против 60. Важнее `core`: Слой 1 читает README каждого кандидата,
и анонимных 60 в час не хватило бы даже на один скан. Публичного метода
для `/rate_limit` у `GitHubClient` нет — при разовой проверке дёргался `_request`.

## Не запушено

Ничего: локаль и `origin/main` — зеркало. Пуш делается только по явной просьбе.

## Окружение

- Системный Python 3.9.6 не годится (нужен 3.11+). Стоит 3.12.14 через `uv`:
  `pip3 install --user uv`, `uv python install 3.12`. Homebrew на машине нет.
- Работать через `.venv` проекта: `.venv/bin/pytest`, `.venv/bin/ruff`.
- **pre-commit требует venv в PATH:** `PATH="$PWD/.venv/bin:$PATH" git commit ...`,
  иначе хук pytest падает с «Executable pytest not found».
- `gh` авторизован под `76MakeKop`. Репозиторий приватный, локаль и origin — зеркало.
